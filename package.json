{
  "name": "perfect-pitch",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:transcription": "node scripts/copy-transcription-assets.mjs",
    "predev": "npm run prepare:transcription",
    "dev": "vite",
    "prebuild": "npm run prepare:transcription",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "test:browser": "playwright test",
    "benchmark": "node --experimental-strip-types scripts/benchmark-pitch.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "onnxruntime-web": "1.29.0",
    "vexflow": "5.0.0"
  }
}
