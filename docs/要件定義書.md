# 要件定義 — Issue #13 リニューアル

## 目的

歌ったり普通に話した声の音程を見て、その声をピアノに置き換えて聴き直す。声の動きを見て・聴いて楽しむ体験を中心にする。既存Vite/TypeScript、GitHub Pages基盤を維持する。

承認元: [Issue #13](https://github.com/santa928/perfectPitch/issues/13)。初版要件を以下で追跡する。仕様の削除/保留はせず、評価未完了を「達成」に読み替えない。

## 初版からの要件差分・受け入れ条件

| ID | 要件 | 差分 | 現時点の状態・証拠 |
| --- | --- | --- | --- |
| REQ-001 | Vite/TS/Pages維持、責務分離 | 維持 | 入力/解析/音符/再生/UIを分離。workflow/base変更なし |
| REQ-002 | 旧/YIN/MPMの精度・追従・負荷比較 | 維持 | 同条件合成PCM比較済み。実端末CPU比較は未検証 |
| REQ-003 | 環境音校正・音量/周期性/前後関係・不明区間 | 維持 | 300ms校正/状態保存/音符化時孤立octave補正。10dB雑音は未達 |
| REQ-004 | サンプル時計、連続MIDI、PCM保持、再解析 | 維持 | 実装・chunk境界同値テスト。録音末尾の半窓制約あり |
| REQ-005 | 歌/話設定、2種ピアノ、休符/短音/連続音程 | 維持 | ±30c/100ms/跳躍/休符の合成回帰。実声の自然な聴感評価は未完了 |
| REQ-006 | 共通再生位置、失敗再試行、停止/切替時解放 | 維持 | 自動ブラウザ・境界テスト。OS電話中断等は未検証 |
| REQ-007 | 音名軸、安定レンジ、実データ、単純操作、mobile/a11y | 維持 | UI刷新。代表viewport証拠。全実機/a11y網羅監査は未完了 |
| REQ-008 | 許諾実音声・静室/定常音/突発音・限界・実機 | 維持 | PJS実音声で経路評価。正解F0・物理雑音・対象実機は未検証 |
| REQ-009 | README/仕様/機能/UI/ライセンス同期 | 維持 | 対応docs更新、音源と音声送信を区別 |
| REQ-010 | build/test/review、証拠付きPR | 維持 | PR #14は後続のユーザー承認でmerge/deploy済み。Issue #13はOPEN |
| REQ-011 | 五線譜化・カタカナのドレミ併記 | 追加 | 後続依頼。表示専用の推定楽譜、固定ド・オクターブ・半音、テンポ調整、休符・タイ。[記譜検証報告](evaluation/score-verification.md)に自動検証と未検証を区別 |
| REQ-012 | 提供録音の両モードで倍音誤りと細切れ音を調整 | 追加 | 候補比較・入力抑制・派生音符を修正。[前回評価](evaluation/pitch-continuity.md)、追加歌唱の[1フレーム候補比較](evaluation/period-continuity.md)。誤判定・途切れは残り、追加版の実機と最終聴感は未確認。REQ-001〜011の削除/保留はない |

旧ターゲット合わせ・基準音・練習スコア・固定ダミー音階はIssue #13で承認された非目標/主導線整理に従って除外。公開基盤・純粋な周波数/音名変換の関係式は維持し、テスト付きで分離する。

## 性能・精度目標（実装前に設定）

- 55〜1000Hz、44.1/48kHz、位相/振幅/倍音を変えたクリーン合成音: median誤差5c以下、p95 20c以下、大誤差50c超/オクターブ誤り1%以下、取りこぼし5%以下。
- 合成無音・非周期雑音の有声誤検出1%以下。
- 100ms音を保持。歌/話の因果追従150/120ms以内。Dockerの1窓CPU p95 10ms以内。音声I/O・転送・描画遅延は別途扱う。
- 描画30/60/120fpsでも解析のsample時刻と音符長が変わらない。±30cは連続モードで保持、半音丸めのみ変化する。
- 最大60秒PCM。録音/再解析/音源の失敗と停止後に操作可能状態へ戻る。

[合成評価](evaluation/synthetic-report.md)には達成値と強い雑音の未達を併記する。旧検出器単体と新pipeline全体を同じ誤検出率として混同しない。

## 非対象

原曲採点/正解メロディの取得、音源分離、声とピアノの同時ライブ再生、MIDI出力、共有・アカウント、別repo/フレームワーク移行、不要なbackend。楽譜追加ではPDF出力、自動テンポ/拍子/調推定、楽譜通りの量子化再生は含まない。新変更のmainマージ・本番公開・Issueクローズは実施しない。

## 楽譜追加の受け入れ条件と性能目標

- 録音後に実データから五線譜と固定ドのカタカナを表示。無音に偽の音符を出さない。
- 4/4、16分単位、仮BPM120（40〜240の整数で変更可）、最初の検出音を1拍目と明示する。
- 小節内の音符と休符は16 tick、長音の分割だけタイを付ける。丸めで消えた短音を件数表示する。
- 原PCM・連続音高・既存ピアノの発音時刻は変更しない。再解析/再録音/設定変更中の古い描画は破棄する。
- 60秒・BPM240で最大60小節、描画は1ページ4小節まで。ライブラリは必要時だけ読み込む。
- 375px/1280pxで音名の重なり・SVGの下端/右端の欠け・ページ全体の横はみ出しを検査する。
- 既存REQ-001〜010の削除・保留はない。楽譜追加を音声解析の精度改善として扱わない。

## リスクと対策

- 低音の観測時間と短い抑揚: 窓長を比較し歌/話を分ける。境界の時間誤差を明示。
- 周期的環境音と声の区別: 保証しない。物理環境音試験と複数音源は未完了。
- マイク/音声中断・非対応: 明示停止・エラー、勝手に再開しない。
- 音源取得/デコード: timeout/再試行。外部JSを実行しない。簡易音で代替しない。
- メモリ: 60秒上限、PCMコピーの用途を限定。実端末でのピークメモリは未計測。
- 実音声ラベル不足: 検出有声割合を「精度」や「検出率」と呼ばない。独立F0正解が揃うまで精度未検証。
- 採譜の不確実性: 表示は近似と明示。BPMを調整可能にし、消えた短音を報告。半音表記は♯へ統一し、調性の推定はしない。

## 完了前チェック

- [x] 受け入れ条件を保持
- [x] 非対象を明示
- [x] リスクと対策を保持
- [x] 性能目標を保持
- [ ] 実機評価・実声精度など全受け入れ条件を達成（PR #14マージ後も未完了）
